import { ScormAgent } from "./presentation/ScormAgent";

// If you need to provide agentContext (for env bindings, etc.), define it here
export const agentContext = {};

export { ScormAgent as Chat };
